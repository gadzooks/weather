-unless alerts.blank?
  %a{name: 'all-alerts'}
  - alerts.sort.each do |alert|
    %div{class: 'alert-details alert-' + alert.severity}
      %span.alert_id.title{id: alert.alert_id}
        %a{href: root_path(anchor: 'top')}
          =alert.alert_id
          &uarr;
      %span.title
        = alert.title
      .regions
        %span.center.region
          = "Regions Impacted : "
        = alert.regions.join(', ')
      .description
        %span.center.description
          = "Details : "
        =alert.description
